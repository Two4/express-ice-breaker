import {User, UserBase} from "../user/user.interface";
import {UserRecord} from "../user/userrecord.interface";

// Read
export const getUser = async (user: UserBase): Promise<User> => {/*TODO*/}
export const getUserRecord = async (user: UserBase): Promise<UserRecord> => {/*TODO*/}